
export const selectFriends = (state) => state.friends.friends;

export const selectIsLoadingFriends = (state) => state.friends.isLoading;

export const selectIsErrorFriends = (state) => state.friends.isError;
