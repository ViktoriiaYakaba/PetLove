export const selectCategories = state => state.notices.categories

export const selectPetSex = state => state.notices.sex

export const selectPetTypes = state => state.notices.types

export const selectNotices = state => state.notices.notices

export const selectFavoritesIds = state => state.notices.favIds

export const selectLastPage = state => state.notices.lastPage

export const selectSexValue = state => state.notices.sexValue

export const selectSortWord = state => state.notices.sortWord

export const selectIsLoading = state => state.notices.isLoading

export const selectErrorNotices = state => state.notices.error

export const selectCurrentNotice = state => state.notices.currentNotice
